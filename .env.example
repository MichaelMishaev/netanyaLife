# ===================================
# Netanya Local - Environment Variables
# ===================================
#
# Copy this file to .env.local for development
# For production, set these in Vercel dashboard
#
# IMPORTANT: Never commit .env files with real secrets!
#

# ===================================
# Database (PostgreSQL)
# ===================================
# Development: Docker container (port 5433) or local PostgreSQL
# Production: Vercel Postgres, Railway, Neon, or Supabase
DATABASE_URL="postgresql://netanya_user:netanya_password_dev@localhost:5433/netanya_local?schema=public"

# ===================================
# Redis (Cache & Rate Limiting)
# ===================================
# Development: Docker container (port 6380) or local Redis
# Production: Upstash Redis (recommended for serverless)
REDIS_URL="redis://localhost:6380"

# ===================================
# Authentication & Security
# ===================================
# JWT Secret for admin session tokens
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="change-this-to-a-secure-random-secret-min-32-chars-in-production"

# ===================================
# Application URLs
# ===================================
# Base URL (used in sitemap, structured data, emails)
# Development: http://localhost:4700
# Production: https://netanya-local.com
NEXT_PUBLIC_BASE_URL="http://localhost:4700"

# ===================================
# Google Analytics
# ===================================
# Google Analytics 4 Measurement ID (format: G-XXXXXXXXXX)
# Get from: https://analytics.google.com → Admin → Data Streams
# Only loads in production (NODE_ENV=production)
NEXT_PUBLIC_GA_MEASUREMENT_ID="G-XXXXXXXXXX"

# ===================================
# Node Environment
# ===================================
NODE_ENV="development"

# ===================================
# Production Checklist
# ===================================
# Before deploying:
# 1. ✅ DATABASE_URL → production database
# 2. ✅ REDIS_URL → production Redis (Upstash)
# 3. ✅ JWT_SECRET → strong random secret
# 4. ✅ NEXT_PUBLIC_BASE_URL → production domain
# 5. ✅ NODE_ENV → "production"
# 6. ✅ Run: npx prisma migrate deploy
# 7. ✅ Run: npx prisma db seed (initial data)
